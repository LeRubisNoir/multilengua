require 'spec_helper'

describe "VocableGroups" do
  describe "GET /vocable_groups" do
    it "works! (now write some real specs)" do
      get vocable_groups_path
      expect(response.status).to be(200)
    end
  end
end
