require 'spec_helper'

describe "Vocables" do
  describe "GET /vocables" do
    it "works! (now write some real specs)" do
      get vocables_path
      expect(response.status).to be(200)
    end
  end
end
